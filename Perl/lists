
Question: Print a list with commas
    print join(',', @array);
    
    my @array = qw(one two three);
    {
        local $, = ',';
        print @array;
    }
    
    sub commify_series {
        (@_ == 0 ) ?    ''                     :
        (@_ == 1 ) ?    $_[0]                  :
        (@_ == 2 ) ?    join( " and ", @_)     :
                        join(", ", @_[0 .. ($#_ -1)], "and $_[-1]");
    }
        
